# FastAPI & ASGI
fastapi==0.115.0
uvicorn[standard]==0.30.6

# DB (PostgreSQL, SQLAlchemy)
SQLAlchemy==2.0.35
psycopg[binary]>=3.2.2,<3.3
pymilvus==2.4.*          # 2.4 대 유지: 서버/클라이언트 호환 안정

# 요청/응답
httpx==0.27.2
pydantic==2.9.2
python-multipart==0.0.20

# YOLO (Ultralytics, PyTorch)
ultralytics==8.3.10
# ✅ Torch/TV는 반드시 호환쌍으로 고정하세요 (CPU 빌드 기준)
torch==2.3.1
torchvision==0.18.1

# 이미지/과학 계산
numpy==1.26.4            # OpenCV/DeepSORT와 호환 안정 (NumPy 2.x로 올리면 충돌 가능)
opencv-python==4.10.0.84
Pillow==10.4.0           # (필수) PIL 사용

# Tracking (옵션)
deep-sort-realtime==1.3.2
scipy==1.11.4 
# Windows + Python 3.11/3.12에서 DeepSORT가 lap 에러 나면 아래 설치 추가:
# lapx>=0.5.5 ; platform_system == "Windows"

# (옵션) 임베딩 품질 향상: OpenCLIP
# open-clip-torch==2.24.0
